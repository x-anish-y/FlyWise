# ============================================================
# FlyWise (APIx) — Python dependencies
# ============================================================

# HTTP requests (Bright Data & Scrappa adapters)
requests>=2.31,<3

# Environment variable management
python-dotenv>=1.0,<2

# HTML parsing (OTA scraper fallback)
beautifulsoup4>=4.12,<5

# Browser automation (OTA scraper)
playwright>=1.40,<2

# PostgreSQL driver (ingestion pipeline → Neon)
psycopg2-binary>=2.9,<3

# FastAPI + ASGI server (API)
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.0,<3
